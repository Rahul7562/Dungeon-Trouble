Title: "🧪 [Add MathModule tests]"

Description:
* 🎯 **What:** Created a new test file `MathModuleTests.cpp` in `Tests/Math` to provide test coverage for the `MathModule.cpp` file. The test scaffolding verifies that `initMath()` executes without throwing exceptions.
* 📊 **Coverage:** The initialization of the math module (`initMath()`) is now covered under a basic happy path test case, ensuring it can be executed safely.
* ✨ **Result:** Improved overall test coverage and established a foundational scaffold for adding future tests for the math module.
