#include "InputManager.h"

namespace DungeonEngine::Input {
    // The implementation is fully inlined in the header for this class since it's very simple.
    // However, keeping the cpp file for future explicit specializations or complex setups.
}
